services:
  library:
    build: ./library
    image: grozddv/testrepo:library
#    volumes:
#      - ./library/src/DB/:/app/src/DB/
    ports:
      - "3000:3000"
  counter:
    build: ./counter
    image: grozddv/testrepo:counter
    ports:
      - "3001:3001"
#    volumes:
#      - ./counter/src/:/app/src/
